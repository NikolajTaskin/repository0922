export function createNode(nodeName, attributes) {
	const element = document.createElement(nodeName);

	for (const attributeName in attributes) {
		inputElement.setAttribute(attributeName, input[attributeName])
	}

	return node;
}